# Project rules bridge

This repo's canonical conventions live in:
- .claude/CLAUDE.md
- .claude/rules/*

When working in this repo, ALWAYS read and follow those rules.
If there is a conflict between .agent rules and .claude rules, prefer .claude rules unless the user explicitly says otherwise.
